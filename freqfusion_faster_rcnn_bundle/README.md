# Paquete de archivos: Faster R-CNN + FreqFusion

Esta carpeta contiene copias directas de los archivos de Python y scripts shell necesarios para reproducir la integración de FreqFusion dentro de Faster R-CNN sobre COCO.

Se generan automáticamente ejecutando:

```bash
python tools/export_freqfusion_faster_rcnn.py
```

Si necesitas actualizar los contenidos, vuelve a ejecutar el comando anterior para regenerar las copias.
